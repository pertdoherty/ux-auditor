version: "3"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    depends_on:
      - redis

  worker:
    build: ./apps/worker
    depends_on:
      - redis
